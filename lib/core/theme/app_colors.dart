import 'package:flutter/material.dart';

class AppColors {
  // ───── Brand Seed ─────
  static const seed = Color(0xFF0FBF9F);
  static const primarySeed = seed;

  // ───── Primary Set ─────
  static const primary = Color(0xFF0FBF9F);
  static const onPrimary = Color(0xFFFFFFFF);
  static const primaryContainer = Color(0xFFBDF3E6);
  static const onPrimaryContainer = Color(0xFF00382D);

  // ───── Secondary / Accent ─────
  static const secondary = Color(0xFF006D77);
  static const onSecondary = Color(0xFFFFFFFF);
  static const secondaryContainer = Color(0xFFB2EBF2);
  static const onSecondaryContainer = Color(0xFF002C30);

  // ───── Background / Surface ─────
  static const background = Color(0xFFF6F8F9);
  static const onBackground = Color(0xFF0E1B2A);
  static const surface = Color(0xFFFFFFFF);
  static const onSurface = Color(0xFF0E1B2A);
  static const surfaceVariant = Color(0xFFE5E9ED);
  static const onSurfaceVariant = Color(0xFF4B5A6B);

  // ───── Neutrals / Borders / Disabled ─────
  static const outline = Color(0xFFCBD5E1);
  static const disabled = Color(0xFFE2E8F0);
  static const shadow = Color(0xFF000000);

  // ───── Semantic / Status ─────
  static const success = Color(0xFF22C55E);
  static const warning = Color(0xFFF59E0B);
  static const error = Color(0xFFEF4444);
  static const info = Color(0xFF3B82F6);

  // ───── Dark Mode ─────
  static const darkBackground = Color(0xFF0E1B2A);
  static const darkSurface = Color(0xFF1C2632);
  static const darkPrimary = Color(0xFF0AA58B);
  static const darkOnPrimary = Color(0xFFFFFFFF);
  static const darkTextPrimary = Color(0xFFF6F8F9);
  static const darkTextSecondary = Color(0xFFC5CCD4);
  static const darkOutline = Color(0xFF334155);
}
